import styled from "styled-components";

const FilterWrapper = styled.div`
  width: 100%;
  padding: 20px 0;

  .filterLabel {
    font-weight: 700;
    display: block;
  }
  .filterInput {
    display: block;
    width: 100%;
    padding: 5px;
  }
`;

export default FilterWrapper;
